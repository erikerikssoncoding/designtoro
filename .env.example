# Express server port
PORT=3002

# Email sender configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
# Set to "true" when using port 465
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Optional sender address (defaults to office@designtoro.ro if not set)
MAIL_FROM=office@designtoro.ro
