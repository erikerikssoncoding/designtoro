# Server settings
PORT=3002

# Email sender configuration
# Configure from server environment; keep secret values only here.
SMTP_HOST=mail.exemplu.ro
SMTP_PORT=587
SMTP_USER=noreply@exemplu.ro
SMTP_PASS=schimba_cu_parola_ta
SMTP_SECURE=false
MAIL_FROM=noreply@exemplu.ro

# Optional API rate limiting for /api/* routes
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=25
